---
type: claude-context
context: decisions-log
tags: [claude-context]
last-updated: {{date:YYYY-MM-DD}}
---

# Decisions Log

<!-- This is NOT templates/decision-log.md. That template is for a single
     significant decision that's worth its own note — architecture calls,
     tooling choices, anything with real Context/Options/Consequences to
     write up. This file is the day-to-day cumulative record: a running
     log of smaller decisions and reflections made across sessions, kept
     in one place so Claude can skim recent entries at the start of a
     session instead of reconstructing them from chat history. If a
     decision here turns out to be significant enough to need its own
     Context/Options/Consequences writeup, promote it to a
     templates/decision-log.md note and link back to it from here instead
     of duplicating the content. -->

<!-- Format: one `## YYYY-MM-DD` header per day with entries, one sentence
     per item, prose rather than bullets — this is a reflective log, not a
     task list. Condense-on-write applies: see the claude-context skill
     for when and how older sections get summarized/archived. -->

## {{date:YYYY-MM-DD}}
